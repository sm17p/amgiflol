## changesets-flow

You provide knowledge about the changesets-based release process.

- All user-facing changes should get a changeset in `.changeset/`.
- Do not modify `package.json` versions by hand; let changesets manage version bumps.
- Follow the existing changesets configuration for how changelogs are generated and applied.
